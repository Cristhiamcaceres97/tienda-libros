/* const corsAnywhere = require("cors-anywhere");

const host = "0.0.0.0";
const port = 8080;

corsAnywhere
  .createServer({
    originWhitelist: [],
  })
  .listen(port, host, () => {
    console.log(
      `Servidor proxy CORS Anywhere en funcionamiento en http://${host}:${port}`
    );
  });
 */